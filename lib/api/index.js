const channels = require('./channel')
const deployments = require('./deployment')
const environments = require('./environment')
const machines = require('./machine')
const packages = require('./package')
const processes = require('./process')
const projects = require('./project')
const releases = require('./release')
const variables = require('./variable')

module.exports = {
  channels,
  deployments,
  environments,
  machines,
  packages,
  processes,
  projects,
  releases,
  variables
}
